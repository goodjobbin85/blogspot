<h2 align="center">Create New Category</h2>

<div align="center">
	<%= form_for(@category) do |f| %>
		<p>
			<%= f.label "Name" %>
			<%= f.text_field :name %>
		</p>

		<p>
			<%= f.submit %>
		</p>

	<% end %>
</div>
